#include <iostream>
int main(int argc, char **argv) {
	/*
	 * Wat zijn de waarden van de attributen pianocat, ceilingcat en happycat na het uitvoeren van de code?
	 * ceilingcat = ceilingcat() = (7,5,4,9)
	 * happycat = pianocat + ceilingcat:(230,10,6, 1) + (7, 5, 4, 9) / 2= (118,12,5, 5)
	 * pianocat =* ceilingcat: (230, 10, 6 , 1) * (7, 5, 4, 9) =  (1610, 50, 24, 9)
	 * */
	return 0;
}
